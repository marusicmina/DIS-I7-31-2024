server:
  port: 8083
  error:
    include-message: always

spring:
  application:
    name: catalog-service

  datasource:
    # Podrazumevane vrednosti vaze SAMO za lokalno pokretanje iz IDE-a (baza je
    # kroz docker-compose mapirana na host port 5435). Unutar Docker-a
    # docker-compose postavlja CATALOG_DB_HOST=catalog-db i CATALOG_DB_PORT=5432.
    url: jdbc:postgresql://${CATALOG_DB_HOST:localhost}:${CATALOG_DB_PORT:5435}/${CATALOG_DB_NAME:catalogdb}
    username: ${CATALOG_DB_USER:catalog}
    password: ${CATALOG_DB_PASSWORD:catalog}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  # Povuci zajednicku konfiguraciju sa config-servera.
  # "optional:" znaci da servis radi i ako config-server nije pokrenut.
  config:
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}

eureka:
  client:
    enabled: true
    serviceUrl:
      # Lokalno iz IDE-a: Eureka na localhost. U Docker-u: docker-compose
      # postavlja EUREKA_URL na http://eureka-server:8761/eureka/
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
    # Kraci intervali nego podrazumevani (30s) - u razvoju hocemo da servis
    # osvane u registru za par sekundi, a ne za pola minuta.
    registryFetchIntervalSeconds: 5
    initialInstanceInfoReplicationIntervalSeconds: 5
  instance:
    # Registruj se preko IP adrese umesto hostname-a; u Docker-u je to pouzdanije.
    preferIpAddress: true
    leaseRenewalIntervalInSeconds: 5
    leaseExpirationDurationInSeconds: 10
